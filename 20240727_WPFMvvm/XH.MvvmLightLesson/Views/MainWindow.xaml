<Window x:Class="XH.MvvmLightLesson.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:XH.MvvmLightLesson"
        mc:Ignorable="d" DataContext="{Binding Source={StaticResource Center},Path=MainWin}"
        xmlns:vm="clr-namespace:XH.MvvmLightLesson.ViewModels"
        Title="MainWindow" Height="450" Width="800">
    <!--d:也是设计时的显示，运行不显示-->
    <!--<d:Window.DataContext>
        <vm:MainViewModel />
    </d:Window.DataContext>-->
    <!--<Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>-->
    <StackPanel>
        <TextBox Text="{Binding Value}" />
        <TextBox Text="{Binding Value}" />
        <Button Content="Button" Command="{Binding BtnCommand}"/>
        <Button Content="Button Param" Command="{Binding BtnParamCommand}" CommandParameter="123"/>
        <ScrollViewer>
            <ItemsControl ItemsSource="{Binding ValueList}" 
                          ScrollViewer.HorizontalScrollBarVisibility="Auto"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"/>
        </ScrollViewer>
    </StackPanel>
</Window>
